# SocketTCPSeguroEmLinux
Socket TCP Seguro (Linux)  Aqui é feita uma comunicação via Socket TCP em C (no Linux) de modo seguro, utilizando OpenSSL (algoritmo de encriptação / decriptação AES256 CBC).
